<!-- res/values/themes.xml -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.BATTicket" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- 기본 색상을 중립적으로 설정 -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- 필요 시 secondary 등도 추가 -->
        <!-- <item name="colorSecondary">@color/kakao_yellow</item> -->
    </style>

    <style name="Theme.BATTicket" parent="Base.Theme.BATTicket" />
    
    <!-- 커스텀 버튼 스타일 -->
    <style name="CustomButton" parent="Widget.Material3.Button.UnelevatedButton">
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:textAllCaps">false</item>
    </style>
</resources>